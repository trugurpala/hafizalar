# Support

Use this order when you need help:

1. Read [`docs/INSTALLATION.md`](docs/INSTALLATION.md).
2. Run `npm.cmd test`.
3. Run the installer with `--dry-run`.
4. Open a GitHub issue with the command output, operating system, Node version, selected surface, and expected result.

Do not include credentials, private customer data, or local `.env` contents.
